function mazeData = HW4P6_generate_maze(size)
    % Create a new maze with a random generation algorithm
    mazeData = ones(size, size);
    % (Include code to create paths in the maze, e.g., using depth-first search)
    maze = generate_maze(m, n)
end